$(document).ready(function () {
    var scrollT;
    var timer = 0;

    var $gnb = $('#gnb');

    //문서가 로드되면 #header.load를 추가해서 브라우저 상단에서 아래로 내려오게

    $('#header').addClass('load');
    $('#content .top_visual').addClass('on');
    
})